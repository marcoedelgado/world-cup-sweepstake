export const FEATURES = Object.freeze({
  bracket:        true,
  transformAlbum: true,
  groupStandings: true,
  ownerDetail:    true,
});

// Radial-bracket owner colours, assigned by owner order in owners.json.
export const OWNER_PALETTE = Object.freeze([
  '#e4572e', // 1
  '#17bebb', // 2
  '#8bc34a', // 3
  '#4d9de0', // 4
  '#e15fed', // 5
  '#f2b705', // 6
]);
